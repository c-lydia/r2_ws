version: '3.8'

services:
  perception:
    runtime: nvidia
    ipc: host
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=all
      - MODEL_PATH=/workspace/models/yolov8n.onnx